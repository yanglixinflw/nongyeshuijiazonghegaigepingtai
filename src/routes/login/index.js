import React from 'react';
import { routerRedux, Route, Switch } from 'dva/router';
function IndexPage() {
  return (
    <div>
      登录页
    </div>
  );
}

IndexPage.propTypes = {
};

export default IndexPage;
